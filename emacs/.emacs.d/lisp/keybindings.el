;;; keybindings --- package-unrelated keybindings

;;; Code:
(windmove-default-keybindings 'meta)
(global-set-key (kbd "RET") 'newline-and-indent)

(provide 'keybindings)

;;; keybindings.el ends here
